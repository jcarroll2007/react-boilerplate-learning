// import { fromJS } from 'immutable';
// import { makeSelectVisualizeDomain } from '../selectors';

// const selector = makeSelectVisualizeDomain();

describe('makeSelectVisualizeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
